# /dashboard is kept out of search results by its noindex meta tag, NOT by a
# Disallow rule — blocking crawl here would prevent Google from ever
# seeing the noindex.
User-agent: *
Allow: /

Sitemap: https://www.trackratsprint.club/sitemap.xml
